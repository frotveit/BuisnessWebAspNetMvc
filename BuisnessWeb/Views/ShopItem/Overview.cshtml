@model ShopOverviewViewModel


@await Html.PartialAsync("Carousel")

<h2>Shop items of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div class="row">

    @foreach (var item in Model.ShopItemsOfTheWeek)
    {
        @Html.Partial("ShopItemOfTheWeekSummary", item)
    }

</div>